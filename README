En la clase del once de febrero Carlos nos enseño a manejar github.
yo me comprometo a no volver a preguntar como se usa:
1.git clone
2.git add
3.git commit
4.git status (esto se lo debemos a Luis )
recordarle a carlos RSA
